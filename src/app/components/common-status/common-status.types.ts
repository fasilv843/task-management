/** Visual intent a status chip communicates — maps 1:1 onto the app's shared intent palette. */
export enum StatusTone {
  Warning = 'warning',
  Info = 'info',
  Success = 'success',
  Danger = 'danger',
}
